[solution]
name = SnakeGame

include = 3rdparty/3rdparty.buildscript

[project:SnakeGame]
type = exe
if(Windows)
{
    subsystem = Windows
}
sources = src/**/*.cpp
headers = src/**/*.h
includes = src
target_link_libraries(SDL3)
std = 20